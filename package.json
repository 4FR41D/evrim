{
  "name": "evrim",
  "version": "0.1.0",
  "private": true,
  "description": "Kendini sürekli geliştiren, hafızalı AI uygulaması (GitHub bağlantılı, mobil öncelikli)",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "prestart": "node -e \"try{require.resolve('express')}catch(e){process.exit(1)}\" || npm install --no-audit --no-fund",
    "start": "node server.js",
    "dev": "node --watch server.js",
    "push": "node scripts/push.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}